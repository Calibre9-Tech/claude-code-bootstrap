# {{PROJECT_NAME}} -- Claude Configuration

> Auto-generated by project-bootstrap on {{DATE}}. Edit this file to keep it current.

## Repository

- **Repo:** {{GITHUB_REPO_URL}}
- **Main branch:** {{MAIN_BRANCH}}
- **Lint:** `{{LINT_COMMAND}}`
- **Test:** `{{TEST_COMMAND}}`

## Project Overview

{{PROJECT_DESCRIPTION}}

**Stack:**
- **Framework:** {{FRAMEWORK}}
- **Language:** {{LANGUAGE}}
- **Database:** {{DATABASE}}
- **UI Library:** {{UI_LIBRARY}}
- **Deployment:** {{DEPLOYMENT_PLATFORM}}

## Superpowers Workflow

This project uses the [Superpowers plugin](https://npmjs.com/package/superpowers-extended-cc) for structured development workflows.

| Need | Command |
|------|---------|
| New feature idea | `/superpowers-extended-cc:brainstorming` |
| Plan work | `/superpowers-extended-cc:writing-plans` |
| Execute plan | `/superpowers-extended-cc:executing-plans` |
| Write tests | `/superpowers-extended-cc:test-driven-development` |
| Debug | `/superpowers-extended-cc:systematic-debugging` |
| Isolate work | `/superpowers-extended-cc:using-git-worktrees` |
| Review | `/superpowers-extended-cc:requesting-code-review` |
| Ship | `/superpowers-extended-cc:finishing-a-development-branch` |

## Subagents

Specialized agents for this project. Claude delegates to them automatically for the relevant tasks.

| Agent | When to use |
|-------|-------------|
| `general-assistant` | Code editing, refactoring, debugging, general tasks |
| `database-specialist` | Schema changes, migrations, queries, RLS policies |
| `playwright-tester` | E2E tests, test failures, browser automation |
| `code-reviewer` | Pre-merge review -- tags issues as Critical/Major/Minor |
| `security-auditor` | OWASP audit before shipping auth or data-handling features |

Invoke explicitly when needed:
```
Use the database-specialist to add a users table
Use the playwright-tester to write tests for the login flow
Use the code-reviewer to review my changes before I push
```

## Slash Commands

| Command | What it does |
|---------|-------------|
| `/commit` | Lint, stage (no credentials), conventional commit |
| `/run-ci` | Iterate lint + tests until clean |
| `/whats-next` | Generate session handoff doc |
| `/fix-pr` | Pull and fix all PR review comments |
| `/summarize` | Summarize current session |

## Verification Gate

Never claim a task is complete without concrete evidence:

| Change type | Required evidence |
|-------------|------------------|
| Code change | Run relevant test/lint, show output |
| UI change | Take a screenshot |
| API change | Show request/response |
| DB migration | Show migration ran, show query result |
| Deployment | Show git hash + CI status |

No "should work", "try it now", or "this should fix it" responses.
Run the verification yourself and show the result.

## Tool Priority (Token Efficiency)

Use the right tool to avoid burning tokens:

| Task | Preferred | Avoid |
|------|-----------|-------|
| Read file | `Read` tool | `Bash cat` |
| Search content | `Grep` tool | `Bash grep/rg` |
| Find files | `Glob` tool | `Bash find/ls` |
| DB schema | Targeted `information_schema` query | `list_tables` (14k tokens) |

When using Playwright MCP, prefer screenshots (~400 tokens) over full DOM snapshots (~15k tokens).

## Development Workflow

1. **Plan** -- use `/superpowers-extended-cc:writing-plans` for any change > 30 minutes
2. **Implement** -- use feature branches (`git checkout -b feat/<name>`)
3. **Verify** -- run `/run-ci` before pushing
4. **Commit** -- use `/commit` for conventional commits
5. **Review** -- use `code-reviewer` agent before merging (team projects)
6. **Ship** -- use `/superpowers-extended-cc:finishing-a-development-branch`

---

{{TECH_SPECIFIC_SECTIONS}}

---

## Quick Reference

{{QUICK_REFERENCE_TABLE}}
